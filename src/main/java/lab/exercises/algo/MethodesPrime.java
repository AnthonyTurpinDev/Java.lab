public class MethodesPrime {
    public static void main(String[] args) {
        // Consigne : crée isPrime(n) qui retourne true si n est premier.
        int n = 29; // Exemple : vérifier si 29 est premier
        if (isPrime(n)) {
            System.out.println(n + " est un nombre premier.");
        } else {    
            System.out.println(n + " n'est pas un nombre premier.");
        }

    }
}